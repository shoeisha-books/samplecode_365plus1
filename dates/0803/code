// 100以下の素数をExcelに書き込むプログラム
// --- 素数一覧を収集 ---
素数一覧は「」
数を、1から、100まで、繰り返す(
　　もし、素数判定(数)ならば(
　　　　素数一覧＝素数一覧＆数＆改行
    )
)
// --- Excelに書き込む ---
エクセル起動。
素数一覧を、(0, 0)へ、エクセル一括設定。
デスクトップのパス&「素数.xlsx」へ、エクセル保存。
エクセル終了。
終わる。
// --- 素数判定する関数を定義 ---
＊素数判定(?の)
　　Nに、1番目の、引数得る。
    素数=はい
　　Iを、2から、((N^0.5)+1)まで、繰り返す(
    　　もし、N%I=0ならば(素数=いいえ)
    )
　　それ=素数。
    戻る。

